<div class="form-handle"><i class="material-icons">drag_handle</i></div>

<%= form_with(model: child_post, local: true) do |form| %>
  <% if child_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(child_post.errors.count, "error") %> prohibited this child_post from being saved:</h2>

      <ul>
      <% child_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field body">
    <%= form.text_area :body, id: :child_post_body, placeholder: 'body' %>
  </div>

  <div class="field file">
    <%= form.file_field :picture, id: :child_post_picture %>
  </div>

  <%= form.hidden_field :root_post_id %>

  <div class="actions submit">
    <%= form.submit "Post"%>
  </div>
<% end %>
